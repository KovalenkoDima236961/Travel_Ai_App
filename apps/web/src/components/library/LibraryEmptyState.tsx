export function LibraryEmptyState({ mode }: { mode: "archived" | "completed" | "results" | "default" }) { const content = { archived: ["No archived trips", "Archived trips you hide from active lists will appear here."], completed: ["No completed trips yet", "Completed trips will appear here after their end date."], results: ["No results found", "Try changing filters."], default: ["Your travel library is empty", "Past trips, recaps, and reusable plans will appear here."] }[mode]; return <div className="rounded-2xl border border-dashed border-sand-400 bg-white/60 px-6 py-14 text-center"><h2 className="font-newsreader text-2xl font-semibold text-cocoa-900">{content[0]}</h2><p className="mt-2 text-sm text-cocoa-500">{content[1]}</p></div>; }
