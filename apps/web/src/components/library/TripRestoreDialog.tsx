export function TripRestoreDialog({ destination, pending, onConfirm, onClose }: { destination: string; pending?: boolean; onConfirm: () => void; onClose: () => void }) {
  return <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-cocoa-900/35 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl"><h2 className="font-newsreader text-2xl font-semibold text-cocoa-900">Restore this trip to your active trip list?</h2><p className="mt-2 text-sm leading-6 text-cocoa-600">{destination} will be shown again when its lifecycle matches your regular trip list.</p><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} type="button" className="rounded-full px-4 py-2 text-sm font-medium text-cocoa-600">Cancel</button><button onClick={onConfirm} disabled={pending} type="button" className="rounded-full bg-cocoa-900 px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{pending ? "Restoring…" : "Restore trip"}</button></div></div></div>;
}
