import { useState } from "react";

export function TripArchiveDialog({ destination, pending, onConfirm, onClose }: { destination: string; pending?: boolean; onConfirm: (reason?: string) => void; onClose: () => void }) {
  const [reason, setReason] = useState("completed");
  return <div role="dialog" aria-modal="true" className="fixed inset-0 z-50 grid place-items-center bg-cocoa-900/35 p-4"><div className="w-full max-w-md rounded-2xl bg-white p-6 shadow-xl"><h2 className="font-newsreader text-2xl font-semibold text-cocoa-900">Archive this trip?</h2><p className="mt-2 text-sm leading-6 text-cocoa-600">Archived trips are hidden from your active trip list but remain available in your library. This does not delete trip data.</p><p className="mt-3 text-sm font-medium text-cocoa-800">{destination}</p><select value={reason} onChange={(event) => setReason(event.target.value)} className="mt-4 w-full rounded-xl border border-sand-300 px-3 py-2 text-sm"><option value="completed">Completed</option><option value="abandoned">Abandoned</option><option value="duplicate">Duplicate</option><option value="other">Other</option></select><div className="mt-6 flex justify-end gap-3"><button onClick={onClose} type="button" className="rounded-full px-4 py-2 text-sm font-medium text-cocoa-600">Cancel</button><button onClick={() => onConfirm(reason)} disabled={pending} type="button" className="rounded-full bg-clay px-4 py-2 text-sm font-semibold text-white disabled:opacity-60">{pending ? "Archiving…" : "Archive trip"}</button></div></div></div>;
}
