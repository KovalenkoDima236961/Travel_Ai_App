import type { AIGenerationPromptSnapshot } from "@/entities/ops/model";
export function PromptSnapshotViewer({ snapshot }: { snapshot?: AIGenerationPromptSnapshot | null }) { if (!snapshot) return null; return <section><div className="flex items-center gap-2"><h3 className="text-[14px] font-semibold text-cocoa-800">Prompt snapshot</h3><span className="rounded-full bg-[#FAEFDA] px-2 py-0.5 text-[11px] font-semibold text-[#96682A]">Redacted prompt</span></div><p className="mt-2 text-[12.5px] text-cocoa-500">Sensitive fields are redacted. Raw prompts are not stored.</p><pre className="mt-3 max-h-80 overflow-auto rounded-xl border border-sand-200 bg-sand-50 p-4 text-xs leading-5 text-cocoa-700">{snapshot.contentRedacted}</pre></section>; }
