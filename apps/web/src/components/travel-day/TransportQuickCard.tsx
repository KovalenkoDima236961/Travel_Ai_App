import type { TravelDaySummary } from "@/types/travel-day";
export function TransportQuickCard({ route }: { route: TravelDaySummary["route"] }) { const transport = route.selectedTransportSummary[0]; if (!transport) return null; return <section className="rounded-2xl border border-sand-300 bg-white p-4"><p className="text-xs font-semibold uppercase tracking-wide text-cocoa-500">Selected transport</p><p className="mt-1 font-semibold text-cocoa-900">{transport.mode} · {transport.operatorName || transport.serviceName || "Travel option"}</p><p className="mt-1 text-sm text-cocoa-500">{transport.departureTime || "Time to confirm"} → {transport.arrivalTime || ""}</p></section>; }
