import Link from "next/link";
export function TravelDayEmptyState({ tripId, mode }: { tripId: string; mode: string }) { return <section className="rounded-3xl border border-dashed border-sand-400 bg-white p-8 text-center"><h1 className="font-newsreader text-3xl font-semibold text-cocoa-900">{mode === "post_trip" ? "Trip completed" : "No plan for today"}</h1><p className="mt-2 text-sm text-cocoa-500">Open the planner to review the full itinerary.</p><Link className="mt-4 inline-flex rounded-full bg-clay px-4 py-2 text-sm font-semibold text-white" href={`/trips/${tripId}`}>Open planner</Link></section>; }
