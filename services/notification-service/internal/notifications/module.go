package notifications

// This file is intentionally small: the notification service is constructed via
// New (service.go). It exists to mirror the self-contained "module" layout used
// by Trip Service's activity/usercontext packages, keeping the package shape
// familiar across the codebase.
