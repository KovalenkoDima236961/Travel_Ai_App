# VCS / tooling
.git
.gitignore
.dockerignore

# Docker / Compose
Dockerfile
docker-compose.yml
docker-compose.*.yml

# Docs
*.md

# Env / secrets
.env
.env.*

# Local Go caches & build artifacts (never needed in the image)
.cache/
bin/
tmp/
coverage.out

# Local private exports must never enter an image build context.
data/exports/
backups/
exports/
*.key
*.pem
